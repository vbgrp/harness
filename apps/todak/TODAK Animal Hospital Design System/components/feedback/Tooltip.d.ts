/** Hover hint on Frost 900. */
export interface TooltipProps {
  children?: React.ReactNode;
  label?: React.ReactNode;
  placement?: 'top' | 'bottom';
  style?: React.CSSProperties;
}
export function Tooltip(props: TooltipProps): JSX.Element;
