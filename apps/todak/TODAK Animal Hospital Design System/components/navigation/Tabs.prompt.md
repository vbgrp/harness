Switch between sibling views.

```jsx
<Tabs variant="underline" items={['진료안내','의료진','오시는 길']} value={tab} onChange={setTab} />
```

`pill` inside cards and app screens; `underline` for page-level navigation.
