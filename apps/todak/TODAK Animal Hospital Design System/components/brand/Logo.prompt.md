Brand mark. Never redraw or recolour it beyond the two supplied tones.

```jsx
<Logo variant="symbol" height={40} />
```

Minimum clear space equals the sprout height. Symbol-only below 40px tall; lockup at 56px+ so the EN subtitle stays legible.
