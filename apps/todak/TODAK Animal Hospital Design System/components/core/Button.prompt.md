Primary action control — always fully pill-rounded, never square.

```jsx
<Button variant="primary" size="lg" icon="calendar-check">진료 예약하기</Button>
```

One Spicy `primary` per view; `secondary` (frost) for the second action, `outline`/`quiet` inside cards, `ghost` in nav. Press state shrinks to 0.97.
