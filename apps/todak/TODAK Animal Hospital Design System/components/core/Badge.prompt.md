Status pill. Soft tint by default; `solid` only when it must read on imagery.

```jsx
<Badge tone="mint" icon="check">접종 완료</Badge>
```

Tones map to meaning: mint = done/healthy, accent = highlight, cta = urgent/emergency, calm = informational.
