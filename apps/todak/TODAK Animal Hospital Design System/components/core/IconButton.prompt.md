Icon-only circular button for utility actions.

```jsx
<IconButton name="phone" variant="solid" label="전화 문의" />
```

Always circular (pill radius). Use `quiet` inside cards, `solid` for a floating call action.
